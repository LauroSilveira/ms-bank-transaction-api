server:
  port: 8080


spring:
  application:
    name: ms-transaction-bank-api

  h2:
    console:
      enabled: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:transactiondb
    username: admin
    password:

